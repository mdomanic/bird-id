# Core
python-dotenv>=1.0        # load configuration from .env
# tflite-runtime is built against NumPy 1.x and crashes on NumPy 2
# (AttributeError: _ARRAY_API not found), so pin below 2.
numpy>=1.24,<2            # array handling for the local classifier

# Local bird-ID engine (BIRD_ID_ENGINE=local, the free default).
# tflite-runtime is Linux/macOS only; on Windows use BIRD_ID_ENGINE=claude
# or `pip install tensorflow` instead.
tflite-runtime>=2.14 ; sys_platform == "linux"

# Claude bird-ID engine (BIRD_ID_ENGINE=claude). Optional unless you use it.
anthropic>=0.40           # Claude vision API

# Arlo camera access (unofficial cloud API)
pyaarlo>=0.8

# Frame extraction from recorded clips + image handling
opencv-python-headless>=4.9
pillow>=10.0

# Web dashboard
flask>=3.0

# Desktop notifications (Windows toast). Optional; ignored on other platforms.
winotify>=1.1 ; sys_platform == "win32"
